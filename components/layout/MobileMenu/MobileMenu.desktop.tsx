'use client';

interface MobileMenuDesktopProps {
  isOpen: boolean;
  onClose: () => void;
}

export default function MobileMenuDesktop({ isOpen, onClose }: MobileMenuDesktopProps) {
  return null;
}